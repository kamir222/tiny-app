<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <style>
        html {
          background: white;
          background: #E8EAF6;
        }
        body {
          font-family: 'Montserrat', sans-serif;
          color: white;
          margin: 20px auto;
          max-width: 680px;
          height: 600px;
          padding: 60px;
          background: #3F51B5;
          box-shadow: 6px 4px 41px 2px rgba(0,0,0,0.75);
          border-radius: 5px;
        }
        button {
          margin: 4px 2px;
          cursor: pointer;
          padding: 5px 20px;
        }
        a {
          color: #FFEA00;
        }
  </style>
</head>
<body>
<%- include('partials/_header') %>
  <ul>
    <li><%= shortURL %> <%= longURL %>
      <form method="POST" action="/urls/<%= shortURL %>">
        <input type="text" name="url" placeholder="<%= longURL %>">
        <button type="submit">Update</button>
      </form>
    </li>
  </ul>
</body>
</html>
