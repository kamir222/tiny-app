<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <!-- Add your EJS code here -->
    <ul>
    <% for (key in urls ) { %>
        <li><%= key %> <%= urls[key] %></li>
    <% } %>
    </ul>
    <a href="#">link for shortening a new URL</a>
</body>
</html>
